{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main UV Studio window",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
